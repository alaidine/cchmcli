[project]
name = "cchmcli"
version = "0.1.0"
description = "A minimal CLI tool for screencasting using aiortc WebRTC"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argparse>=1.4.0",
    "pillow>=12.0.0",
    "pyaudio>=0.2.14",
    "vidstream>=0.0.14",
]
