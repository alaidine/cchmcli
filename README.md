# cchm (Chrome Cast Home Made)

### Usage

Receiver:
```
# To start the receiver
$ uv run receiver.py
stop # Type stop in the terminal to stop the receiver
```

Sender:
```
# To send screencast to the receiver
$ uv run sender.py <receiver_ip>
# To send screencast to localhost
$ uv run sender.py
```
